# Actual result status

The paper values were extracted and checked as 11 function rows. A fresh Table 4 run is not claimed: the workspace does not contain the common input grid, raw outputs, or one unified harness for all seven published baselines. The available SCNA trainer and partial baseline implementations are preserved under the bundle's first-level `train/` directory for auditability.
