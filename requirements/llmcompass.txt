# First-party lock for the dependencies used by the bundled LLMCompass snapshot.
# torch is pinned by accuracy.txt, SCALE-Sim is installed from the bundled source,
# and torchvision is intentionally absent because no artifact path imports it.
seaborn==0.13.2
absl-py==2.3.1
configparser==7.2.0
numba==0.61.2
pandas==2.3.1
cython==3.1.2
